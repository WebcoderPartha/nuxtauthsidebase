{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@sidebase/nuxt-auth": "^0.6.0-beta.2",
    "@types/node": "^18",
    "nuxt": "^3.5.1"
  },
  "dependencies": {
    "sweetalert2": "^11.7.5"
  }
}
